# -------------------------------
# OS / Editor cruft
# -------------------------------
.DS_Store
Thumbs.db
*.swp
*.swo
*~
.idea/
.vscode/
.history/
.fleet/

# -------------------------------
# Build artifacts (repo layout)
# -------------------------------
artifacts/
dist/
build/
bin/
coverage/
*.tmp

# -------------------------------
# Go
# -------------------------------
# (Go modules: nothing special. Keep go.mod/go.sum.)
# If you produce local binaries under ./src/**/:
src/**/*.exe
src/**/*.dll
src/**/*.so
src/**/*.dylib

# -------------------------------
# Rust
# -------------------------------
# Keep Cargo.lock (recommended for binaries/apps).
target/
**/target/
# If you create cross-target dirs under src:
src/**/target/

# -------------------------------
# Packer
# -------------------------------
packer/packer_cache/
packer/*.log
packer/manifest.json
packer/*.pkr.hcl.hcl      # rare temp file
# Local var files (keep an example checked in)
packer/*.pkrvars.hcl
!packer/*.pkrvars.hcl.example

# -------------------------------
# Terraform
# -------------------------------
terraform/.terraform/
terraform/.terraform.lock.hcl # <-- KEEP this file committed!
!terraform/.terraform.lock.hcl

terraform/terraform.tfstate
terraform/terraform.tfstate.*
terraform/*.tfstate.backup
terraform/crash.log
terraform/override.tf
terraform/override.tf.json
terraform/*_override.tf
terraform/*_override.tf.json

# Private var files (keep .example committed)
terraform/*.tfvars
terraform/*.auto.tfvars
terraform/envs/**/terraform.tfvars
!terraform/*.tfvars.example
!terraform/*.auto.tfvars.example

# Optional local outputs
terraform/*.plan

# -------------------------------
# Salt
# -------------------------------
# (Keep all .sls, templates, and files in repo.)
# Ignore local caches/logs if you copy any during debugging:
salt/.cache/
salt/*.log

# -------------------------------
# Logs & runtime
# -------------------------------
*.log
logs/
*.pid

# -------------------------------
# Env files / direnv
# -------------------------------
.env
.env.*
.envrc

# -------------------------------
# Misc
# -------------------------------
*.bak
*.old
*.orig


